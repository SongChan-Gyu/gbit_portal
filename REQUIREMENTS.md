# GBIT Portal — 요건·룰 정리

여기까지 구현된 휴가·숙소·스탬프·결재·관리 기능의 **비즈니스 규칙과 요건**을 한 문서로 정리한 것입니다.

**문서 유지 원칙:** 요건이 바뀌거나 기능이 추가/변경되면 **반드시 이 파일(REQUIREMENTS.md)을 함께 수정**할 것. 기억만 하지 말고 여기에 **상세·정확하게** 적어 두어야 나중에 참고할 수 있음.

---

## 0. 화면별 표시·노출 규칙 (요건 요약)

| 화면 | 규칙 |
|------|------|
| **팀 월간 일정** (대시보드·팀 일정 페이지) | 일자별로 **누가**(이름) + **오전/오후/휴가** 표시. ADMIN/PM은 소속 팀 없어도 **전사** 승인 휴가 기준으로 표시. 팀 일정 월간 뷰에서 ADMIN/PM이면 해당 일자 전원 표시, 일반은 최대 5명. |
| **내 휴가 현황** | **연차**는 기본연차·근속가산·이월을 **한 줄 "연차"**로 통합 표시(부여/사용/잔여 합산). 1/5/10년 근속휴가는 별도 행 유지. 상단 **총 부여·사용·잔여** 숫자는 **소모 가능 자산**(연차 통합·돌봄·연휴연장·포상·근속특별·생일·직무부서 등 `LeaveAllocation` 기준)만 합산하며, **공가·병가 성격은 합계에 넣지 않음**(유형별 상세에서 확인). **신청 내역** 목록은 카드형: 상단에 유형·기간·일수·**상태 배지**, 하단에 **철회/취소신청**과 **상세보기**(결재·사유) 2열. |
| **휴가 신청** | 상단 KPI **사용 가능 휴가**는 연차 풀·돌봄·연휴연장·포상·근속특별·생일·직무부서 등 **자산 부여 잔여 합**(공가·병가 제외). 휴가 종류 선택은 UI에서 **자산형**(연차·돌봄·연휴연장·스탬프·하프데이·포상·생일반차)과 **사유형**(공가·인정·병가) **섹션 분리**. |
| **제주도 숙소** | 입실일만 "매월 1일 기준 2달 후 말일" 한도. **퇴실일**은 입실일+1박 ~ 입실일+최대연박(기본 14일)까지 선택 가능(퇴실일이 6월이어도 입실 5월이면 허용). 과거 일자 신청 가능. |
| **로그아웃** | 로그아웃 시 **현재 접속한 origin**(예: localhost, ngrok URL) 기준으로 `/login`으로 리다이렉트. (이전에 캐시된 ngrok 등으로 가지 않도록) |
| **결재함 > 휴가 결재 (대기)** | **리스트는 보기만**: 신청자명 · `M/D(요) · 유형 N일` 요약 한 줄 · `[N단계]`만 표시, **승인/반려 버튼 없음**. **상세 패널**에서만 의견 입력 + 승인/반려(취소 신청은 취소 승인/반려). 모바일은 목록 상단·얇은 행, 선택 시 하단(스크롤) 상세; PC는 좌측 목록·우측 상세 2열. |
| **결재함 > 스탬프 승인** | 휴가 결재와 동일하게 **리스트 = 보기만**(이름 · `날짜 · 내용 요약` · 상태 뱃지), **상세 = 의견 + 승인/반려**. `/stamp/approve` 단독 화면에도 동일 컴포넌트 적용. |

---

## 1. 휴가 (귀속·연차·유형)

### 1.1 귀속연도

- **기간:** 매년 **5월 1일 ~ 다음해 4월 30일**
- 연차·돌봄·연휴연장 등 대부분 휴가 유형은 이 귀속연도 기준으로 부여·사용

### 1.2 연차 (기본·근속·이월)

- **정규직**
  - **기본:** 15일 (입사 1년 이상)
  - **근속 가산:** 2년마다 +1일, **최대 25일**
  - **1년 미만(입사 첫해):** 월별 1일씩 발생 (별도 처리), 최대 11일
- **프리랜서:** 15일 고정
- 연차 풀 = **기본연차(BASE_ANNUAL) + 근속가산(TENURE_BONUS) + 이월연차(CARRYOVER)**  
  → 특별휴가(1/5/10년 근속)·부서추가는 별도 풀

### 1.3 휴가 유형 (`prisma/seed-base.ts` 기준)

**결재:** **1단** = 팀장만 · **2단** = 팀장 → PM · **0** = 결재 없음.

| 코드 | 이름 | 단위 | 연차 차감 | 결재 | 비고 |
|------|------|------|-----------|------|------|
| ANNUAL | 연차 | 1일 | O | **1단** | |
| AM_HALF / PM_HALF | 연차(오전/오후반차) | 0.5일 | O | **1단** | |
| CONDOLENCE | 경조휴가 | 1일 | X | **2단** | 부여일기준 |
| CARE / CARE_AM / CARE_PM | 돌봄휴가 | 1일·0.5일 | X | **1단** | **연 2일** 한도, 별도 풀 |
| PUBLIC / PUBLIC_AM / PUBLIC_PM | 공가·오전·오후 공가 | 1일·0.5일 | X | **2단** | |
| RECOGNITION / RECOGNITION_AM / RECOGNITION_PM | 인정휴가 | 1일·0.5일 | X | **2단** | |
| PM_HALF_MONTH | 하프데이(오후 인정) | 0.5일 | X | **1단** | **월 1회** |
| SICK | 병가 | 1일 | X | **2단** | |
| HEALING_DAY | 힐링데이 | 0일 | X | **0** | **스탬프 5개** 사용 |
| PM_RECOG_STAMP | 오후인정(스탬프) | 0.5일 | X | **1단** | **스탬프 10개** |
| TENURE_1Y / 5Y / 10Y | 1/5/10년 근속휴가 | 1일 | X | **1단** | 입사일기준 12개월 유효 |
| AWARD | 포상휴가 | 1일 | X | **2단** | 부여일기준 12개월 |
| HOLIDAY_EXT | 연휴연장휴가 | 1일 | X | **1단** | **귀속연도 1일** 한도, 별도 풀 |
| BIRTHDAY_HALF | 생일반차 | 0.5일 | X | **1단** | 해당 월 0.5일 부여(스케줄러) |

### 1.3.1 전용 부여 풀 (`LeaveType.allocationSourceCode`)

- 휴가 유형 행에 **`allocationSourceCode`**(선택)를 두면, 신청·승인 시 차감되는 **`LeaveAllocation.sourceCode`** 와 **동일한 문자열**인 할당(관리자 부여·귀속연도 초기화 등)에서만 일수를 차감한다.
- **연차 차감(`deductFromBalance: true`)** 과 동시에 쓰지 않는 것이 일반적이다. 전용 풀만 쓰는 유형은 **연차 미차감** + **`allocationSourceCode` = 해당 부여 코드**(예: `CARE`, `HOLIDAY_EXT`, `BIRTHDAY_HALF`, `AWARD`)로 맞춘다.
- **시스템 동작 요약**
  - 신청 API·승인 시: 해당 유형이 가리키는 부여 행에서 `usedDays` 반영; **연차 풀**(기본·근속·이월)로는 **전용 풀 부족을 대체하지 않는다**(만료 연장 등도 연차 소스끼리만).
  - 클라이언트(휴가 신청 화면)도 동일 기준으로 잔여를 합산·검증한다.
- **차감 일수 계산:** 대부분은 반차 0.5일·종일은 신청 일수; **연휴연장(`allocationSourceCode`가 `HOLIDAY_EXT`인 종일)** 은 시스템에서 **1일 고정**으로 풀에서 차감한다(규정: 귀속 1일 · 연속 사용 1일 단위).
- **새 휴가 종류(관리자 부여만으로 쓰는 유형) 추가 시:** DB/시드 또는 **관리 > 휴가 설정 > 휴가 유형**에서 `allocationSourceCode`와 `LeaveAllocation` 부여 시 같은 `sourceCode`만 맞추면 된다. **타입별 API 분기를 계속 늘릴 필요는 없다**(스탬프·힐링 등 **별도 연동이 있는 유형**은 예외).
- **신청 화면 그룹(`LEAVE_GROUPS`):** 주요 유형을 코드로 그룹에 묶으며, 화면에서는 **자산형**(부여 일수를 쓰는 유형)과 **사유형**(공가·인정·병가 등)으로 **섹션을 나눠** 표시한다. 새 유형을 노출하려면 해당 코드를 그룹 정의에 반영한다(유형만 DB에 있으면 API는 동작할 수 있으나 UI에서 찾기 어려울 수 있음).

### 1.4 경조·조의 휴가 (규정 안내)

- 본인 결혼 5일, 자녀 결혼 1일, 배우자 출산 10일  
- 형제·자매/배우자 부모 결혼 1일  
- 부모상/배우자 부모상 9일, 배우자상/자녀상/조부모상 5일  
- 형제·자매상/배우자 형제·자매상 3일, 그 외 친족상 1일  

(실제 부여·차감은 인사/관리자가 할당한 경조휴가 풀 기준)

### 1.5 휴가 신청 시 검증 규칙

- **과거 일자:** 휴가 신청은 **과거 일자도 선택·신청 가능** (피치 못해 사후 입력·재신청 시 사용).
- **공휴일·주말:** **각 신청 항목**의 시작일~종료일에 대해 **주말 또는 공휴일이 포함되면 신청 불가**. 한 번에 여러 항목을 넣을 때 **전체 최소~최대 날짜 사이에 비어 있는 주말·공휴일**은 검사 대상이 아님(항목별 실제 신청일만 검증).
- **연차 풀:** 기본+근속+이월 합산 잔여 이상만 사용 가능, 초과 시 에러
- **관리자 부여 전용 풀:** `LeaveType.allocationSourceCode`가 설정된 유형은 **해당 `sourceCode`의 `LeaveAllocation` 잔여**만큼만 사용 가능(한 건에 여러 행이면 동일 소스 기준으로 합산해 검증). 예: 돌봄 `CARE`, 연휴연장 `HOLIDAY_EXT`, 생일반차 풀 `BIRTHDAY_HALF`, 포상 `AWARD` 등. **신규 전용 풀 유형도 동일 메커니즘.**
- **연휴연장휴가:** 귀속연도 1일 한도(부여 풀), **1일 단위**만 신청 가능(종일 유형).
- **생일반차:** 0.5일 신청만 허용, 해당 월 부여 풀(`BIRTHDAY_HALF`) 잔여 필요
- **하프데이:** 월 1회 한도 (maxPerMonth=1)
- **스탬프 휴가:** 힐링데이 5개, 오후인정(스탬프) 10개 필요·사용 시 차감 (`allocationSourceCode`와 무관한 별도 로직)

### 1.6 결재 라인 (결재라인별 생성 방식)

- **생성 규칙:** 휴가 유형별 `approvalSteps`(결재 단계 수)가 **같은 항목끼리 한 건**으로 묶이고, **다르면 별도 휴가 신청 건**으로 분리된다.  
  - 예: 연차(1단) + 돌봄(1단) → 한 건, 공가(2단) + 연차(1단) → **별도 두 건**.
- **건별 결재 행(LeaveApproval):**
  - **totalSteps = 0:** 결재 없음 (힐링데이 등).
  - **totalSteps = 1:** 1단계만 → 팀장 1명에게만 `LeaveApproval` 1건 생성.
  - **totalSteps = 2:** 팀장(step 1) → PM(step 2). 신청 시 **팀장 1명에게만** `LeaveApproval`(step 1) 생성. 팀장 승인 시 **다음 단계로 PM** `LeaveApproval`(step 2)를 새로 생성하고, PM이 최종 승인하면 건 완료.
- **자동 승인(결재 행 없이 바로 APPROVED):**
  - **PM/ADMIN** 본인 신청
  - **팀장** 본인 신청이면서 해당 건이 **1단계만** 있는 경우 (totalSteps ≤ 1)
- 반려 시 **사유 필수**. 승인 시 연차·돌봄 등 해당 할당에서 usedDays 증가.

### 1.7 휴가 취소·취소 결재

- 신청자가 **취소 요청** 시, 기존 결재 라인과 **동일한 순서**로 **취소 결재(LeaveApproval, status=CANCEL_PENDING)** 가 생성됨.  
  각 결재자가 취소 승인 시 다음 단계로 넘어가고, 최종 단계까지 승인되면 휴가 건이 CANCELLED 처리·할당 복원.
- **관리자(ADMIN) 직권 취소:** 모든 휴가 결재에 대해 직권 취소 가능. 할당(연차 등) 복원.

---

## 2. 매년 귀속연도 초기화

- **실행:** 관리 > 휴가 관리 > 휴가 할당 탭에서 해당 귀속연도 선택 후 **귀속연도 일괄 초기화** 실행. (API: `POST /api/admin/fiscal-year/init`, body: `{ fy: number, dryRun?: boolean }`)
- **대상:** ACTIVE/INVITED 사원. **이미 해당 귀속연도에 같은 sourceCode 할당이 있으면 건너뜀(skip).**
- **부여 내용 (연차 말고도 돌봄·연휴연장·직무부서 포함):**
  1. **기본연차(BASE_ANNUAL):** 입사 1년 이상 15일, 1년 미만은 귀속연도 내 잔여 개월 수(최대 12일) 월별 기본연차.
  2. **근속가산(TENURE_BONUS):** 2년마다 +1일, 최대 10일.
  3. **돌봄휴가(CARE):** 전원 **2일** (귀속연도마다 초기화 시 부여).
  4. **연휴연장휴가(HOLIDAY_EXT):** 전원 **1일** (귀속연도마다 초기화 시 부여).
  5. **직무부서휴가(DUTY_DEPT):** 직급부서가 **운영부(OPERATIONS)·교육부(EDUCATION)·복지부(WELFARE)** 인 사원만 **2일**.
- dryRun=true 시 DB 변경 없이 추가될 할당 목록만 미리보기 반환.

---

## 3. 제주도 숙소

### 3.1 이용 규칙

- **입실·퇴실:** 입실 15시, 퇴실 11시. 1박 = 캘린더 상 endDate > startDate (날짜 기준).
- **예약 가능 기간:** **매월 1일 기준 2달 후 말일까지**가 **예약 시작일(입실일)** 한도.  
  - 예: 3월이면 5/31까지 시작일 선택 가능. **시작일이 이 한도를 넘으면**(그보다 미래면) 신청 불가.  
  - 시스템은 `getJejuBookingWindowEnd()`로 “현재 월 + 2”의 말일을 계산하고, **입실일(startDate)** 만 이 한도 이내인지 검사함.
- **퇴실일:** 입실일 한도와 **무관**. **입실일+1박 ~ 입실일+최대연박**(기본 14일)까지 선택 가능. 예: 3월에 5월 31일 입실 선택 시 14박이면 퇴실 6월 14일까지 선택 가능(6월 퇴실 허용).
- **과거 일자:** 제주 숙소도 **과거 입실일·퇴실일 선택·신청 가능** (피치 못해 사후 입력·재신청 시).
- **최대 연박:** 시스템 설정 `jejuMaxNights` (기본 **14일**).
- **예약금:** **10만원 고정**. 입금자명·입금 계좌는 복지부가 시스템 설정에서 변경 가능.

### 3.2 신청 시 필수 입력

- 이용일(입실일·퇴실일)
- **이름, 연락처, 입실 인원(1명 이상), 입금자명**  
  → 결제/승인 후 정정 불가

### 3.3 차단일·중복

- **예약 불가일:** 시스템 설정 `jejuBlockedDates` (JSON 배열, YYYY-MM-DD). 선택 기간에 **하루라도 포함되면** 신청 불가.
- **중복 신청:** 동일 직원이 **승인 대기(PENDING) 또는 승인(APPROVED)** 인 신청과 **기간이 겹치면** 신청 불가.

### 3.4 결재 (승인 단계)

- **단계:** 제주 숙소는 **1단계 결재**만 존재. 신청(PENDING) → 복지부 또는 PM/ADMIN이 **승인(APPROVED)** 또는 **반려(REJECTED)**. 휴가처럼 팀장→PM 같은 다단계 결재 라인 없음.
- **승인/반려 권한:** **복지부(dutyDept === "WELFARE")** 또는 **PM/ADMIN**
- 반려 시 **사유 필수**
- 복지부는 승인된 예약·승인 대기 신청을 기간별로 조회 가능(일자별 상세); 그 외 직원은 “예약됨” 여부만 조회

### 3.5 취소

- **승인 전(PENDING):** 신청 본인이 취소 가능 → 즉시 CANCELLED, 사유·취소 시각 기록.
- **승인 후(APPROVED):**
  - **본인:** “취소 요청”만 가능 → 상태가 **CANCEL_REQUESTED**로 변경되고, 취소 사유·요청 시각(`cancelRequestedAt`) 저장. **복지부 또는 PM/ADMIN이 취소를 승인해야만** 최종 CANCELLED 처리됨.
  - **복지부/PM:** 승인된 건을 **직권 취소** 가능 → 즉시 CANCELLED.
  - **취소 결재:** 복지부 또는 PM/ADMIN이 **취소 승인 대기(CANCEL_REQUESTED)** 목록에서 “취소 승인” 시 → CANCELLED, “취소 반려” 시 → APPROVED로 복구(취소 요청 취소). API: `POST /api/jeju/cancel-approve` (requestId, action: "APPROVE" | "REJECT").

### 3.6 신청 내용 수정·상세 표시

- **수정:** **승인 대기(PENDING)인 경우에만** 신청 내용 수정 가능. **이용일(입실·퇴실)은 수정 불가** (일자 유효성·중복 등 재검증 부담을 줄이기 위함). 사유·투숙객명·연락처·인원·입금자명만 수정 가능. 결제/승인 후에는 수정 불가. API: `PATCH /api/jeju/update`.
- **내 신청 내역:** 신청자(본인)·인원 수·입금자명·투숙객명·연락처 등 상세 표시. CANCEL_REQUESTED일 때 “취소 요청 중” 안내.
- **승인 화면(복지부/PM):** 신청 목록·테이블에 투숙객명·인원·입금자명·연락처 등 상세 표시. 취소 승인 대기 건은 별도 섹션에서 취소 승인/반려 처리.

---

## 4. 스탬프 쿠폰

### 4.1 적립

- **운영반영일(오전 6시)** 참석 시 스탬프 1개 적립.
- 스탬프 적립은 **팀장 서명(승인)** 필요.

### 4.2 사용

- **5개:** **힐링데이** 1회  
  - 10:20 출근 또는 **16:00 퇴근**(1시간 40분).  
  - **결재 없음**, 이력만 기록.
- **10개:** **오후 인정휴가(스탬프)** 1회  
  - 0.5일 오후 인정, **팀장 승인 필요**. 연차 미차감.

---

## 5. 사원 정보·인사 관리

### 5.1 사번·직급·고용유형

- **사번:** **자동 부여**. 엑셀 일괄 등록·단일 사원 등록 모두 사번을 비워두면 저장 시 `E001`, `E002` … 형식으로 자동 부여. 기존 사번이 있으면 유지.
- **직급:** **사원, 대리, 과장, 차장, 부장, 이사**만 사용. 엑셀·단일 등록 폼 모두 동일. 한글 그대로 저장.
- **직급부서:** 운영부/교육부/복지부/해당사항없음 (한글 또는 코드 입력 가능). 귀속연도 초기화 시 위 3개 부서는 직무부서휴가 2일 부여 대상.
- **고용유형:** **정규직(FULL), 프리랜서(FREE), 외부개발자(EXTERNAL)**.  
  - **외부개발자(EXTERNAL):** 휴가 관련 기능 **전부 미제공** (휴가 신청/결재/관리·귀속연도 초기화 대상 제외). **제주도 숙소** 신청·내역·안내만 사용 가능. 메뉴는 대시보드 + 제주도 관련만 노출.
- **표시:** 인사/조직·엑셀 미리보기에서 직급부서·고용유형·역할은 **한글**로 표시 (코드값 노출 없음).

### 5.2 사원 엑셀 일괄 등록

- **위치:** 관리 > 인사 관리(사원 관리) 화면 내 **엑셀 일괄 등록** 블록.
- **흐름:**  
  1. **엑셀 양식 다운로드** (템플릿: **사번 없음** — 이름, 팀, 직급, 직급부서, 입사일, 생년월일, 연락처, 이메일, 고용유형, 역할).  
  2. **엑셀 파일 선택** → **미리보기** (import-preview)로 행·에러 확인. 미리보기에서 직급부서·고용유형·역할은 **한글**로 표시.  
  3. **일괄 등록** 실행 시 미리보기된 데이터로 사원 생성/갱신. 사번 비어 있으면 자동 부여.
- (테스트: 엑셀 업로드·미리보기·일괄 등록 플로우 구현·검증됨.)

---

## 6. 팀·전체 월간 일정 (표시 요건)

- **대상:** 대시보드 "팀 월간 일정" 블록, 팀 일정 페이지(`/team-schedule`).
- **일자별 표시:**  
  - **"N명"만 보이지 않게** 하고, **누가**(이름) + **오전/오후/휴가**를 각각 표시.  
  - 반차·하프데이 등은 **오전** 또는 **오후**, 종일은 **휴가**로 표시.
- **팀 소속 없을 때:**  
  - **ADMIN·PM**이면 소속 팀이 없어도 **전사**(ACTIVE 사원 전체) 승인 휴가 기준으로 월간 일정 표시.  
  - 대시보드 제목은 "전체 월간 일정", 팀 일정 페이지 제목은 "전체 일정".
- **팀 일정 페이지 월간 뷰:**  
  - **ADMIN/PM**이면 해당 일자에 휴가 쓴 사람 **전원** 표시(갯수 제한 없음).  
  - 일반 사용자는 일자당 최대 5명까지 표시, 초과 시 "+N" 등으로 표시 가능.

---

## 7. 내 휴가 현황 (연차 표시 요건)

- **연차 통합 표시:**  
  - DB에는 기본연차(BASE_ANNUAL)·근속가산(TENURE_BONUS)·이월(CARRYOVER)가 **별도 할당**으로 있으나,  
  - **내 휴가 현황 > 유형별 잔여 상세**·대시보드·휴가 신청 등에서는 이 셋을 **"연차" 한 줄**로만 표시.  
  - **표기:** 구분을 위해 **중간 점(·)** 으로 구분. 예: `연차 (기본 15 · 근속 3 · 이월 0)`.
  - 부여·사용·잔여는 위 세 소스 합산값으로 표시.
- **1/5/10년 근속휴가:** 별도 행 유지(통합하지 않음).
- **상단 요약(총 부여·사용·잔여):** **소모 가능 자산** 할당만 합산한다.  
  - 포함 예: 연차 통합 행(ANNUAL), 돌봄(CARE), 연휴연장(HOLIDAY_EXT), 포상(AWARD), 생일(BIRTHDAY_HALF), 직무부서(DUTY_DEPT), 1/5/10년 근속(TENURE_1Y/5Y/10Y) 등.  
  - **공가·병가**처럼 “개인 자산 일수”로 보지 않는 유형은 이 합계에 넣지 않으며, 안내 문구로 구분한다. **유형별 잔여 상세** 테이블/카드는 기존처럼 할당 단위별로 모두 표시.

---

## 8. 기타 규정 요약 (화면·운영 안내용)

- **돌봄휴가:** 본인/배우자 자녀 간호·돌봄, 조부모 등 포함, 증빙 불필요, 연 2일.
- **하프데이:** 전 직원 월 1회, 오후 인정, 팀장 승인, 연차 미차감.
- **포상휴가:** 인사/PM 주도, 부서 포상 2일, 개인 성과 1~5일.
- **대체휴가:** 공휴일·일요일 업무 시 해당 일수만큼, 연차 미차감.
- **병가:** 진단서 필요, 최대 60일 등 (규정 안내; 실제 사용은 병가 유형 할당·결재에 따름).

---

## 9. 유동 양식(폼)·공지사항

### 9.1 유동 양식 (관리 > 양식 관리)

- **용도:** 건강검진 신청 등 **유동적인 신청서**. 질문(필드)을 관리자가 자유롭게 정의.
- **필드 유형:** **텍스트**(자유 입력), **콤보(선택)**(항목 한 줄에 하나씩 입력). 필수 여부 설정 가능.
- **공개 링크:** 각 양식마다 **고유 URL** (`/f/[slug]`) 부여. **비회원(회원정보 없음)** 도 링크만 있으면 **이름 입력 후** 제출 가능. 외부개발자 등에게 링크 공유해 신청 받기.
- **관리:** PM/ADMIN만 양식 생성·수정·삭제. **제출 목록**에서 항목별로 일괄 조회, **엑셀 다운로드** 지원.
- **클라우드:** DB·API 기반이라 로컬/클라우드 동일 동작.

### 9.2 공지사항

- **작성 권한:** **PM·ADMIN**만 공지 등록·수정 가능. 댓글 기능 없음.
- **편집:** 글씨체·글씨 크기·사진 등 **리치 텍스트(HTML)** 로 입력·저장. 일반 커뮤니티처럼 자유 편집.
- **노출:** **대시보드** 가장 아래에 **최신 공지 2개**만 작은 링크로 노출. 전체 목록·상세는 공지사항 메뉴에서 조회.
- **클라우드:** 이미지 등은 저장 방식에 따라 동일하게 동작하도록 구성 (로컬 전용 경로 사용 금지).

---

## 10. 전 기능 요약 (테스트·검증 대상)

- **휴가:** 신청(결재라인별 분리·자동승인·**항목별** 주말/공휴일 검증·자산/사유 UI 구분)·결재(팀장→PM 2단계)·반려·취소 요청·취소 결재·관리자 직권 취소.
- **제주도 숙소:** 예약(시작일 2달 후 말일 한도, 퇴실일은 입실+최대연박까지)·과거 일자 가능·필수 입력·차단일·중복 방지·복지부/PM 승인·반려·취소.
- **스탬프:** 적립(팀장 승인)·힐링데이(5개)·오후인정(10개, 팀장 승인).
- **근태:** 귀속연도별·월별 휴가 사용 현황, 일별 출퇴근·휴가 표시.
- **관리:** 인사(사원 등록·수정)·**사번 자동 부여**·**사원 엑셀 일괄 등록(한글 표시)**·휴가 유형(**`allocationSourceCode` 포함**)/할당·**귀속연도 일괄 초기화**·휴가 관리·**양식 관리**·**공지사항**·시스템 설정·감사 로그.
- **조직:** 팀·직원 구성, 팀장 지정.
- **표시:** 연차(기본·근속·이월)는 구분자(·)로 읽기 쉽게 표시.
- **계정:** **아이디 찾기**(이름+이메일 → 이메일로 아이디 발송), **비밀번호 찾기**(아이디 → 재설정 링크 1시간 유효), **관리자 비밀번호 초기화**(임시 비밀번호 설정·이메일 발송 선택).

---

## 11. 아이디·비밀번호 찾기 / 관리자 비밀번호 초기화

### 11.1 아이디 찾기

- **경로:** 로그인 화면 하단 "아이디 찾기" → `/find-id`
- **입력:** **이름** + **이메일** (사원 마스터에 등록된 정보와 일치해야 함)
- **동작:** 일치하는 사원·계정이 있으면 **등록된 이메일로 아이디(username)** 발송. 보안상 계정 유무와 관계없이 동일 안내 메시지 반환.
- **이메일 미설정 시:** SMTP 미설정이면 발송 실패. 개발 시 `TEST_EMAIL_OVERRIDE` 로 수신 주소 지정 가능.

### 11.2 비밀번호 찾기

- **경로:** 로그인 화면 "비밀번호 찾기" → `/forgot-password`
- **입력:** **아이디(username)**
- **동작:** 해당 계정의 **등록 이메일로 비밀번호 재설정 링크** 발송. 링크는 **1시간 유효**. 계정 유무와 관계없이 동일 안내 메시지 반환.
- **재설정:** 링크 접속 → `/reset-password?token=xxx` 에서 **새 비밀번호(8자 이상)** 입력 후 저장. 토큰 사용 후 무효화.

### 11.3 관리자 비밀번호 초기화

- **권한:** PM·ADMIN만 가능.
- **위치:** 관리 > 인사 관리(사원 관리) 목록에서 각 사원 행의 **비밀번호 초기화** 버튼 (계정이 있는 사원만 노출).
- **동작:** 관리자가 **임시 비밀번호(8자 이상)** 입력 후 초기화. 선택적으로 **이메일로 임시 비밀번호 발송** 가능. 사원에게 구두/문자로 전달해도 됨.

### 11.4 이메일 발송 설정·형식

- **사용 프로토콜:** **SMTP만** 사용 (발송 전용). POP3는 수신용이라 시스템에서 사용하지 않음.
- **네이버 메일 테스트:** POP3/SMTP 설정에서 "사용함" 선택 후, SMTP 서버 `smtp.naver.com`, 포트 `465`(SSL), **애플리케이션 비밀번호**로 설정. **테스트 이메일 고정:** `TEST_EMAIL_OVERRIDE`에 주소(예: `zx2253@naver.com`)를 넣으면 **발신(from)·수신(to) 모두 해당 주소로 강제**되며, 로컬·클라우드 동일 적용.
- **회사 정형식:** 모든 발송 메일(아이디 안내·비밀번호 재설정·초대·비밀번호 초기화)은 `wrapEmailBody`로 **공통 레이아웃** 적용. 상단(서비스명·회사명), 본문, 하단(자동 발송 안내·© 회사명) 구조로 통일.

---

## 12. 데이터·시스템

- **배포 시 DB:** 기존 데이터 유지, **없는 것만 시드에서 insert** (시드 로직 변경 금지).
- **날짜:** 전 구간 `YYYY-MM-DD` 및 `@/lib/dateUtils`(todayYMD, addDaysYMD, toYMD) 등 일관 사용.
- **역할:** 팀장(team.leader), PM, ADMIN, 복지부(dutyDept=WELFARE), 외부개발자(employeeType=EXTERNAL) 등으로 메뉴·결재 권한 제어.
- **클라우드 배포:** DB·환경변수·상대 경로 사용으로 로컬/클라우드 동일 동작. 로컬 전용 경로·파일 시스템 의존 없이 구성.

### 12.1 사원별 알림 수신 설정

- 사원 마스터(`Employee`)에 **이메일 수신 허용(`emailEnabled`)**, **알림톡 허용(`alimtalkEnabled`)**이 존재하며 기본값은 **미사용(false)**.
- **이메일 발송**(초대/아이디찾기/비밀번호재설정/관리자 초기화 등)은 원칙적으로 `emailEnabled === true`인 경우에만 발송한다.
  - 단, 개발/테스트 환경에서 `TEST_EMAIL_OVERRIDE`가 설정된 경우에는 수신/발신 주소를 강제하고 테스트를 진행할 수 있다.
- **알림톡 발송**(휴가 결재 요청/결재 결과 등)은 원칙적으로 수신자 `alimtalkEnabled === true`인 경우에만 발송한다.
  - 개발/테스트 비용 방지를 위해 `ALIMTALK_ALLOWED_RECEIVER`가 설정된 경우, 해당 번호로만 발송하고 나머지는 SKIPPED 처리한다.

### 12.2 내 정보(사원 본인) 수정

- 경로: `/me`
- 본인은 **이메일 주소·휴대폰 번호**, **카카오 알림톡 수신 허용**을 수정할 수 있다. (API: `GET/PATCH /api/me`)
- **이메일 발송 거부(미수신)**는 본인 설정에서 선택할 수 없다. 아이디/비밀번호 찾기·초대 등 시스템 메일은 등록 이메일로 발송되므로 필수 수신. 주소 변경은 가능.
- **UI:** 이메일은 “시스템 필수”로 표시하고 한 줄로 안내. 알림톡은 제목·한 줄 설명·수신 허용 체크로 간단히 표시.

### 12.3 관리자: 사원 초대 이메일 일괄 발송

- 관리자는 사원 목록에서 복수 선택 후 **초대 이메일을 일괄 발송**할 수 있다.
- 발송 대상은 기본적으로 **ACTIVE가 아닌 사원**이며, `emailEnabled === true`인 사원만 발송한다. (결과: SENT/SKIPPED/FAILED 리포트 반환)

### 12.4 감사 로그(AuditLog) / 요청(접속) 로그(RequestLog)

- **감사 로그(AuditLog)**: 주요 이벤트(사원/공지/양식/휴가신청/제주숙소 등)에서 `writeAudit()`가 호출될 때 기록된다. 자동으로 DB의 모든 CRUD가 기록되는 형태는 아니다.
- **요청(접속) 로그(RequestLog)**: 운영에서 필요 시 API 요청의 **IP·메서드·URL**을 저장한다.
  - 활성화: `REQUEST_LOG_SECRET` 설정 시에만 기록 (미설정 시 미기록)
  - 조회: 관리 > 시스템 설정 > 데이터(원본)에서 `RequestLog` 테이블

### 12.5 데이터(원본) 조회/수정 도구

- 관리 > 시스템 설정 > 데이터 탭에서 DB 테이블을 직접 조회한다.
- **수정 가능 테이블은 제한**한다(시스템 설정/팀/귀속연도 부여 구분/스케줄러 유형).
- 그 외 테이블은 **읽기 전용 + 페이지네이션**으로 조회만 지원한다.

이 문서는 현재 구현된 규칙을 반영한 요건 정리본입니다. **요건 변경 시 이 파일을 함께 수정**하고, 추가 정책도 여기에 상세히 반영할 것.
